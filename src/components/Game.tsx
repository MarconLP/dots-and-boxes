import { type NextPage } from "next";

const Game: NextPage = () => {
    return (
        <main className="flex justify-center items-center h-[100vh] flex-col">
            <div>
                game board
            </div>
        </main>
    );
};

export default Game